# 🎮 Guess the Number

A simple command-line number guessing game written in Python.

---

## 📌 Description

The program randomly selects a number between 1 and 100.  
You must guess the number with as few attempts as possible.  
Your score starts at 100 and decreases by 1 with each wrong guess.  
You can type `q` anytime to quit the game.

---

## ▶️ How to Run

Make sure Python (version 3 or higher) is installed on your system.

Then run the script in the terminal or command prompt:

```bash
python guess_the_number.py
```
<br>

# 🎮حدس عدد

یک بازی ساده‌ی حدس عدد در محیط خط فرمان (ترمینال) با زبان پایتون نوشته شده است.

---

## 📌 توضیحات

برنامه به‌صورت تصادفی یک عدد بین ۱ تا ۱۰۰ انتخاب می‌کند.  
شما باید با کمترین تعداد تلاش ممکن آن عدد را حدس بزنید.  
امتیاز شما از ۱۰۰ شروع می‌شود و با هر حدس اشتباه یک واحد کم می‌شود.  
در هر زمان می‌توانید با وارد کردن `q` از بازی خارج شوید.

---

## ▶️ نحوه اجرا

اطمینان حاصل کنید که پایتون (نسخه ۳ یا بالاتر) روی سیستم شما نصب شده باشد.

سپس فایل بازی را با ترمینال یا خط فرمان اجرا کنید:

```bash
python guess_the_number.py
```
<br>

---
01001101 01001010 01001101 00110001 00110110❤️
